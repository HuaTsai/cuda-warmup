# pixi environments
.pixi/*
!.pixi/config.toml

# cmake build directories (build/, build-debug/, build-release/)
build*/

# profiler reports (commit a text summary, not the binary blob)
*.ncu-rep
*.nsys-rep
